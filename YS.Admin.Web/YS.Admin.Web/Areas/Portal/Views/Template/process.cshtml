@inject Microsoft.AspNetCore.Hosting.IWebHostEnvironment HostingEnvironment
@{
    Layout = "~/Areas/Portal/Views/Shared/_Layout.cshtml";

}

@section header {

    <link rel="stylesheet" href="@Url.Content("~/css/process.css")" />
}
 
<div id="app3" class="wrap">
    <div class="element-wrap">
        <div class="con-main-wrap">
            <div class="title"> @ViewBag.Title </div>
            <div class="label"> @ViewBag.Explanatory </div>
        </div>
        <div class="list">
            <div class="item" v-for="(item,index) in data.tableList" :key="index" :class="index%2==0?'active':''  ">
                <div class="con-main-wrap flex-row">
                    <div class="left">
                        <div class="titles flex-row">{{ item.C1 }}</div>
                        <div class="title-info"> {{item.C2}} </div>
                        <div class="label"> {{item.C3}} </div>
                    </div>
                    <img class="right" :src="item.C4" alt="" />
                </div>
            </div>
        </div>
    </div>

</div>

<script> 
    const App3 = {
        data() {
            return {
                query: {
                    total: 1000,
                    page: 1,
                    size: 10
                },
                data: {
                    tableList: @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(ViewBag.tableList))
                }
            }

        },
        methods: {

        },
        mounted() {
            cx();
            window.addEventListener('resize', function () {
                cx();
            });
            function cx() {
                var Wth = document.documentElement.clientWidth;
                var app = document.querySelector('#app3');
                if (Wth < 1400) {
                    if (navigator.userAgent.indexOf('Firefox') != -1) {
                    } else {
                        document.documentElement.style.zoom = (Wth / 1250) * 100 + '%';
                    }
                } else {
                    // app.style.width = '100%';
                    document.documentElement.style.zoom = '100%';
                }
            }
        },
        beforeUnmount() {

        },
        components: {}
    };
    function app3init() {
        const app3 = Vue.createApp(App3);
        app3.use(ElementPlus);
        app3.mount('#app3');
    }
</script>